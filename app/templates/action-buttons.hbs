{{#if record.status}}
        <span class="glyphicon glyphicon-ok"></span>
{{else}}
        <button {{action 'sendAction' 'action' record}}>
        <span class="glyphicon glyphicon-ok"></span>/<span class="glyphicon glyphicon-remove"></span>
        </button>

{{/if}}